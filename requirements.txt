# --------------------------------------------------------------------------- #
# Inferno core backend dependencies (no heavy ML).
#
# These run the gateway, the worker plumbing, the dummy model, the metrics, and
# the full test suite. Install a model stack on top with ONE of:
#   pip install -r requirements-ml-gpu.txt   # CUDA 12.4
#   pip install -r requirements-ml-cpu.txt   # CPU-only
#
# Pinned to the exact versions verified on Python 3.10 (conda env `test`).
# --------------------------------------------------------------------------- #
fastapi==0.141.1
uvicorn[standard]==0.52.4
starlette==1.6.0
redis==5.2.1
pydantic==2.13.4
pydantic-settings==2.15.0
structlog==26.1.0
prometheus-client==0.26.0
psutil==6.1.1
PyYAML==6.0.2
python-multipart==0.0.32
# Held on the 16.x line ON PURPOSE: websockets 17.x requires Python >=3.11,
# and this project's floor is 3.10 (pyproject requires-python, the 3.10-slim
# Dockerfile, and every CI job). Bumping to 17.1 turned all four backend jobs
# red at the install step. Neither line carries an advisory.
websockets==16.1.1
anyio==4.14.2

# Optional GPU telemetry. The import is guarded, so this is safe on CPU-only
# hosts (it simply reports no GPUs). Tiny, pure-Python NVML bindings.
nvidia-ml-py==12.560.30

# Optional OpenTelemetry tracing (imports are guarded; only used when
# INFERNO_OTEL__ENABLED=true).
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0

# MCP server (exposes the models as agent tools). Kept on the 1.x line ON
# PURPOSE: mcp 2.x renamed FastMCP -> MCPServer, so backend/mcp_server/server.py
# does not import under it. 1.29.1 clears every advisory that affected 1.2.0.
# sse-starlette 3.x is the line that targets starlette 1.x.
mcp==1.29.1
sse-starlette==3.4.8

# Test + load-test tooling.
httpx==0.28.1
pytest==9.1.1
pytest-asyncio==1.4.0
locust==2.32.4
