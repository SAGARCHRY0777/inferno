# --------------------------------------------------------------------------- #
# ML model stack — CUDA 12.4 build (NVIDIA GPU).
#
# Install AFTER requirements.txt:
#   pip install -r requirements.txt
#   pip install -r requirements-ml-gpu.txt
#
# torch/torchvision come from PyTorch's CUDA 12.4 wheel index. At runtime the
# platform auto-selects CUDA when the driver supports it and transparently falls
# back to CPU otherwise (INFERNO_INFERENCE__DEVICE=auto), so this same file works
# even on a box whose driver is too old for cu124 — it just runs on CPU.
# --------------------------------------------------------------------------- #
--extra-index-url https://download.pytorch.org/whl/cu124

torch==2.13.0+cpu
torchvision==0.28.0+cpu
onnxruntime-gpu==1.24.1   # 1.20.1 was pulled from PyPI; this is the nearest patch
onnx==1.22.0
transformers==5.15.1  # 4.x line: sentence-transformers pins transformers<5
tokenizers==0.22.2  # highest stable in transformers 4.57.x's >=0.22.0,<=0.23.0
safetensors==0.7.0
ultralytics==8.4.128
faster-whisper==1.2.1
sentence-transformers==6.0.0
soundfile==0.12.1
pillow==12.3.0
numpy==2.2.6
